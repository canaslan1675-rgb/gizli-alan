# Progress

**2026-09-25 (Fri):** Picked up the scaffold, put it under git (main = initial scaffold, work on `mvp-v1`). Installed Flutter 3.47.5 stable under /workspace/tools and generated the Android project (FLAG_SECURE MainActivity, USE_BIOMETRIC-only manifest, backup disabled, minSdk 28). Replaced the "hide-only" storage with an always-encrypted vault (AES-256-GCM, per-space keys in Keystore-backed secure storage, encrypted index, legacy plaintext migration), PBKDF2 PIN auth with lockout and optional decoy PIN, biometric unlock, auto-lock with picker guard, a real calculator engine with PIN+`=` entry, 3-step onboarding with own-device confirmation and calculator disclosure, virtual-phone home grid with wallpapers, encrypted gallery (import/grid/viewer/export/delete), files, notes with search, settings, and TR/EN strings. 34 tests (crypto, auth, calculator, storage, app flow) pass; `flutter analyze` clean. Docs: PRIVACY.md, docs/PLAY_COMPLIANCE.md (Data safety, listing TR/EN, reviewer notes), DECISIONS.md, BLOCKERS.md. Release APK builds (`flutter build apk --release`, 54.6 MB universal, debug-signed); merged permissions verified: only USE_BIOMETRIC (+ AndroidX internal signature permission), no INTERNET.

**2026-09-25 (Fri, v0.2):** Owner feedback on v0.1. (1) Launcher label is now "Hesap Makinesi" / "Calculator" via string resources with an original adaptive calculator icon (fg/bg/monochrome, generated by `tool/gen_launcher_icon.py`); vault still disclosed in onboarding, ⓘ, listing. (2) "Second phone": Android managed work profile (Shelter/Island model) — Kotlin profile-owner receiver (empty policies, self-removes as main-profile admin), provisioning with Xiaomi/unsupported handling, HMAC-signed cross-profile trampoline, freeze/unfreeze or quiet mode on Lock (option), clone via installExistingPackage → enableSystemApp → profile Play Store fallback, profile Play Store, LauncherApps grid in the vault home, remove; all behind the real-vault unlock. Brief hard-ban wording clarified; Play risk logged. 50 tests pass, analyze clean, arm64 release APK built. Work on branch `v0.2-calculator-workprofile`, PR into `mvp-v1`.

**2026-09-25 (Fri, coordination):** Added COORDINATION.md (TR+EN), `.cursor/rules/05-coordination.mdc`, docs/AGENT_LOG.md; created labels and `task` issues #4–#17 for all remaining work (owner items #11–#17).

**2026-09-25 (Fri, #4):** Vault-only notification list: encrypted per-space event log (imports, exports, deletions, Second phone changes, wrong PIN attempts since last unlock), home tile with unread badge, mark read/clear; no system notifications, no new permissions. 58 tests pass, analyze clean.

**2026-09-25 (Fri, #5):** Pro/payment UI stub: Settings → GizliAlan Pro with planned plans and prices, usage vs. 50-item free limit (display only), buttons explain there are no purchases in this test build; no billing code. Tests pass, analyze clean.

**2026-09-25 (Fri, #8):** Release signing config: optional android/key.properties (template committed, secrets git-ignored), debug-key fallback with warning, owner steps in README; verified with/without a throwaway keystore.
