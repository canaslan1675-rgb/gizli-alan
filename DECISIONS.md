# Decisions log

| Date (TR) | Decision | Why |
|-----------|----------|-----|
| 2026-09-25 | Architecture = research option C + E: in-app encrypted vault + dual-mode (calculator) UI. No Work Profile, launcher role, app cloning or cloud. | MVP scope per research §6 / brief §1. |
| 2026-09-25 | Encryption always on (removed scaffold's "hide-only" default). AES-256-GCM via `cryptography` (+ `cryptography_flutter` native on Android) instead of pointycastle/`encrypt`. | Pure-Dart pointycastle GCM took ~6 s per 5 MB; `cryptography` Dart impl ~0.6 s and native on device. GCM gives integrity too. |
| 2026-09-25 | One random 256-bit data key per space in Keystore-backed secure storage; not wrapped by PIN. | Allows biometric unlock without PIN; PIN change needs no re-encryption. Limitation documented in PRIVACY/README. Future: biometric-bound Keystore key + PIN-wrapped DEK. |
| 2026-09-25 | PIN = 4–8 digits, PBKDF2-HMAC-SHA256 120k iterations on an isolate; lockout after 5 fails (30 s doubling, cap 15 min). Calculator `=` checks don't count as failures. | Calculator users type normal numbers; counting them would lock the owner out. |
| 2026-09-25 | Decoy PIN opens a separate `decoy` space with its own key; biometrics always open the real space; decoy settings hide security options. | Keepsafe-style decoy, disclosed in listing. |
| 2026-09-25 | Calculator entry on by default in onboarding, with explicit disclosure screen + toggle; launcher label stays "GizliAlan"; ⓘ in calculator explains the entry. Removed scaffold's "4× =" / hidden second-launcher ideas. | Play Deceptive Behavior: listing = behaviour, no cloaking. |
| 2026-09-25 | Removed dart-define `DIRECT_VAULT` second-launcher stub; direct entry = turn calculator off. | Stub never worked per-activity; fewer launcher tricks = lower review risk. |
| 2026-09-25 | Imports via system Photo Picker (`image_picker`) and SAF (`file_picker`), export via SAF "save as"; manifest removes all storage/media/camera/mic permissions. | Minimal permissions; no plaintext temp copy by us (image_picker cache copy is deleted after import). |
| 2026-09-25 | Originals are **not** deleted from the phone gallery after import (user is told). | Deleting needs MediaStore write requests; out of MVP scope. |
| 2026-09-25 | FLAG_SECURE always on (not a toggle). allowBackup=false + data-extraction rules exclude everything. | Keys can't be backed up anyway; backups would be useless blobs. |
| 2026-09-25 | minSdk 28. | BiometricPrompt + FragmentActivity without AppCompat theme issues on ≤8. |
| 2026-09-25 | No billing/IAP code in v1; brief pricing (Free 30–50 items, Pro 249 TL one-time / 449 TL yearly) deferred to a UI stub later. | Brief: real payment integration is a BLOCKER-level step. |
| 2026-09-25 | Language via simple map l10n + `L10nScope` InheritedWidget (no gen-l10n). | Keeps scaffold approach, rebuilds open screens on change. |
| 2026-09-25 | **v0.2:** launcher label → "Hesap Makinesi" (values-tr) / "Calculator" (values, default) via `@string/app_name`; recents title matches. Supersedes "launcher label stays GizliAlan". | Owner request (less conspicuous). Play allows disclosed calculator vaults: the app *is* a working calculator and the vault is disclosed in onboarding step 2, the ⓘ dialog, the store listing and reviewer notes. No icon hiding, no alias switching, no reviewer-specific behaviour. |
| 2026-09-25 | Original calculator icon (navy background, light body, dark display, 3×3 keys, mint "=" key) generated from one geometry by `tool/gen_launcher_icon.py` → adaptive fg/bg + monochrome (themed icons) + round/legacy PNGs + 512 px store icon. | Must not copy Google/Samsung/Xiaomi calculator icons or branding; script keeps vector and PNGs identical and reproducible. |
| 2026-09-25 | **v0.2 "Second phone"** = research option A: managed work profile via `ACTION_PROVISION_MANAGED_PROFILE`; `ProfileAdminReceiver` with empty `<uses-policies>` is profile owner of that profile only. Self-deactivates if enabled as a main-profile device admin. Brief §3 wording clarified accordingly. | Owner request; Shelter/Island model is the only legitimate 3rd-party "second phone". No device-owner/main-profile powers. |
| 2026-09-25 | Main ↔ profile calls go through one cross-profile intent filter to a headless `ProfileActionActivity` (enabled only inside the profile), HMAC-SHA256 signed with a 256-bit secret handed over in `EXTRA_PROVISIONING_ADMIN_EXTRAS_BUNDLE`, 2-min window; caller package checked when available. | Only activities can cross profiles without INTERACT_ACROSS_* permissions. Secret in the provisioning intent avoids Shelter's trust-on-first-use race. |
| 2026-09-25 | App cloning: `installExistingPackage` is tried, but AOSP only allows it for *affiliated* profiles (needs a device owner), so on personal phones it falls back to `enableSystemApp` for system apps and otherwise opens the profile's Play Store page for that app. No `REQUEST_INSTALL_PACKAGES` / APK copying. | Verified in AOSP `DevicePolicyManagerService.installExistingPackage` (affiliation check). Avoids a Play-restricted permission. |
| 2026-09-25 | "Close on lock" option (Off / Hide apps [default] / Turn work profile off). Applied only on the **Lock button** (and back-to-lock), undone on the next real-vault unlock. Quiet mode (`requestQuietModeEnabled`) falls back to hiding apps because Android only lets the default launcher/system do it. Background auto-lock does **not** close the profile. | Auto-lock fires whenever a second-phone app launched from the vault comes to the front; freezing then would kill the app the owner is using. Quiet-mode restriction verified in AOSP `UserManagerService.ensureCanModifyQuietMode`. |
| 2026-09-25 | Second phone is only reachable in the unlocked **real** vault (`secondPhoneIfUnlocked`); decoy vault has no tile and locking it closes nothing. | Gate behind unlock; decoy must not reveal the profile. |
| 2026-09-25 | Profile/clone lists use a `<queries>` MAIN/LAUNCHER intent, not `QUERY_ALL_PACKAGES`; `LauncherApps` for cross-profile listing/launching. No new permissions. | Minimal package visibility; Play-friendly. |
| 2026-09-25 | Xiaomi/Redmi/POCO detected by `Build.MANUFACTURER/BRAND`: warning before setup, blocked message with **Second space** / Android 15+ **Private space** fallback. Generic "not allowed" (existing work profile / OEM) and "unsupported" messages likewise. | MIUI/HyperOS often blocks or half-finishes work-profile provisioning (known Shelter/Island issue). |
| 2026-09-25 | **Play policy risk accepted for testing:** a DeviceAdminReceiver / managed-profile DPC can trigger extra review (Device Admin / "Unwanted software" / Deceptive Behavior checks, esp. combined with a calculator label). Mitigations: disclosure everywhere, empty policies, reviewer notes. Fallback: ship second phone only in a side-loaded APK flavor and keep the Play build vault-only. See BLOCKERS.md. | Owner decides before any Play submission. |
| 2026-09-25 | Version 0.2.0+2. | v0.2 test build. |
| 2026-09-25 | Multi-agent coordination protocol (Joi + Cursor agents): GitHub `task` issues with claim/heartbeat/handoff/24 h takeover labels + append-only `docs/AGENT_LOG.md`; see COORDINATION.md and `.cursor/rules/05-coordination.mdc`. Remaining work filed as issues #4–#17 (owner-only items labelled `owner`). | Owner wants both agent sides working in parallel without direct contact; the repo is the only shared channel. |
| 2026-09-25 | `main` is the integration branch (PRs #1–#3 merged by owner request); all agent branches start from and PR into `main`. | Chained PR lines no longer needed; one base keeps both agent sides in sync. |
| 2026-09-25 | **Vault-only notification list (#4):** encrypted `events.gae` per space (cap 200, newest first) with the app's own events: gallery/files import (count), export and delete (item name), Second phone created / app added / removed, and "N wrong PIN attempts since your last unlock". Home tile with unread badge; mark all read, clear all. The app posts **no system notifications** (no POST_NOTIFICATIONS permission) and never reads other apps' notifications. | Brief week 3 "notification isolation" without new permissions or a Notification Listener (hard ban). Failed attempts use a separate counter that survives decoy unlocks and is reported only inside the real vault, so a decoy user never sees it; calculator `=` checks are not counted. Logging is best-effort and never breaks the action. |
| 2026-09-25 | **Pro UI stub (#5):** Settings → "GizliAlan Pro" lists Free (up to 50 items, no ads), Pro one-time 249 TL (~7.99 USD), Pro yearly 449 TL. Buy/Subscribe only show "not available in this test build". No billing library, no network, no new permissions. Free limit = **50**, **displayed with current usage but not enforced**. Shown in both real and decoy vault settings. | Brief §2 week 3 + §4; real billing is a stop point (#15). Not enforcing avoids locking testers out when there is no way to upgrade. Showing it in the decoy too keeps the decoy looking like a normal vault; it reveals nothing. |
| 2026-09-25 | **Release signing (#8):** `android/app/build.gradle.kts` reads optional `android/key.properties` (storeFile relative to `android/` or absolute, storePassword, keyAlias, keyPassword); present → release `signingConfig` uses it, blank values fail the build; missing → warning + debug key. Template `android/key.properties.example`; `.gitignore` covers key.properties, `*.jks`, `*.keystore`. Recommend Play App Signing. | Standard Flutter pattern; agents/CI can still build test APKs without secrets, and the owner only has to drop in a local file (#13). Verified: no file → `CN=Android Debug`; throwaway keystore → its cert; blank password → clear error. |
| 2026-09-25 | **Docs sync (#9):** ticked brief §2 week 1–3 items verified in code; items done in open PRs (#19, #20, #21) are ticked with the PR number; remaining work linked to issues (#6, #12, #13, #15). §6 rewritten to the current state; §5 marked as standing rules (not ticked); §7 "3 weeks of PR history" left open. No scope, ban or pricing changes. | Brief §7 definition of done needs every item ticked or justified; §6 was stale (GitHub login pending). |
| 2026-09-25 | **Device test plan (#7):** `docs/DEVICE_TEST_PLAN.md` (install/permissions, core vault, Second phone steps 3.1–3.14, device matrix, results table). Emulator run **not** done on the agent box (no emulator/system image ≈ 2 GB, no `/dev/kvm` access) → handed off. | Real hardware is the only open verification of the Second phone; a fixed checklist lets the owner (#14) or any agent with an emulator run it the same way. |
| 2026-09-26 | **Brief §6 current-state refresh:** recorded PRs #18–#23 merged into `main`; open agent work = #6 (cursor, push blocked locally) + #7 emulator close; owner #11–#16. No scope, ban or pricing changes. | §6 was stale (still listed #18–#21 as awaiting merge). |
| 2026-09-26 | **#7 emulator close:** re-checked box — `/dev/kvm` exists (`crw-rw---- root:103`) but inaccessible to `box`; no usable emulator/system image without root → agent-side emulator acceptance met via documented reason; physical devices remain #14. | Acceptance allows "documented reason emulator could not run"; avoids a false handoff loop. |
| 2026-09-26 | **Store screenshots (#6, owner spec):** produced by Flutter widget rendering, not an emulator: `test/store_screenshots_test.dart` (skipped unless `--dart-define=STORE_SCREENSHOTS=true`; wrapper `tool/gen_store_screenshots.sh`) pumps the real `GizliAlanApp` with fabricated content (generated pattern images, neutral fake notes, demo biometrics shown as on) and writes 1080×1920 RGB PNGs (own PNG encoder, colour type 2, no alpha) to `docs/store/screenshots/{tr,en}/` in the order 01 calculator + ⓘ, 02 onboarding step 2, 03 vault home, 04 gallery, 05 notes, 06 settings. FLAG_SECURE stays unconditional in MainActivity; no debug switch. No Second phone frame (pending #11); frames 03/06 still show the Second phone tile/settings row of the current single build — regenerate from a Play flavor if #11 chooses the split build. Test fonts: Flutter's Roboto (+ a patched Regular with "ⓘ", since the test engine has no font fallback). ElevatedButton text style got an explicit `fontFamily: 'Roboto'` (Android default anyway) so it doesn't render with the test font. | Owner spec; FLAG_SECURE blocks real screenshots by design, widget rendering keeps release behaviour unchanged and is reproducible. Taken over from the Cursor agent (branch `cursor/6-store-screenshots`, 03413eb, could not push). |
| 2026-09-26 | **Two build flavors (#11, owner: "ikisini de üretip deneyelim"):** Gradle `productFlavors` (dimension `distribution`): `play` = `com.offerforge.gizlialan`, **no Second phone** — receiver, provisioning/profile-action activities, `profile_admin.xml`, strings, `managed_users`/`device_admin` uses-features and the LAUNCHER `<queries>` live only in `android/app/src/full/`, so they are physically absent from the play APK; `full` = `com.offerforge.gizlialan.full` (v0.2 behaviour). Native hook: `FlavorFeatures.attach()` (full → SecondPhoneChannel, play → null). Dart: `lib/flavor.dart` reads `appFlavor` (from `--flavor`) → `--dart-define=FLAVOR` → default **play**; `Flavor.hasSecondPhone` gates vault tile, settings section, `secondPhoneIfUnlocked`, lock/unlock hooks; `L10n.t` uses `<key>_play` texts (onboarding privacy, privacy body, empty notification list). Same launcher label, FLAG_SECURE, permissions and key.properties signing for both; settings footer shows "0.3.0 · Play/Full" (in-app only). Screenshots: `docs/store/screenshots/{play,full}/{tr,en}/`, Play listing uses `play/`; old `{tr,en}/` removed. Version 0.3.0+3; test prerelease `v0.3.0-test`. | Owner wants both variants on a phone to compare; play build removes the Device Admin review risk entirely; separate source sets beat runtime flags because the components are not in the APK at all. Final Play choice stays with the owner (`owner` label kept). |
| 2026-09-26 | **Play follow-ups (#28):** (1) `Flavor.hasProStub` = `full` only (or `--dart-define=PRO_STUB=true`); Settings hides the "GizliAlan Pro" entry in `play`, ProScreen code stays; free limit still display-only. (2) Feature graphic 1024×500 RGB TR/EN from `tool/gen_feature_graphic.py` (imports the icon geometry from `gen_launcher_icon.py`, Roboto from the Flutter SDK cache), caption "GizliAlan · Calculator Vault / Hesap Makineli Kasa · encrypted photos, files and notes behind a real calculator · offline, no internet permission, no ads". (3) `PRIVACY_URL` dart-define (https only) → Settings → Privacy dialog shows the link + "Open in browser" (Kotlin `SystemChannel` `gizlialan/system`, plain `ACTION_VIEW` + `CATEGORY_BROWSABLE`, returns false if no browser → copy-link fallback) + "Copy link"; without it the in-app text only. **No url_launcher** (avoids any plugin-added manifest entries; INTERNET never needed to start a browser). (4) Play screenshots unchanged: the Pro row was below the fold of `06_settings`; regeneration only changed clock/date pixels, so not committed. Version 0.3.1+4, prerelease `v0.3.1-test`. | Implements the agent follow-ups from docs/play-submission (Broken-functionality risk, User Data in-app policy, required feature graphic) without touching FLAG_SECURE or permissions. |
| 2026-09-26 | **Hide work apps while locked (#30, `full` only):** toggle "Kilitliyken iş uygulamalarını gizle" (default on; maps to close mode `freeze`, off = `off`; legacy `off` respected) on the Second phone screen + Settings. Hidden from inside the profile with `setApplicationHidden` on: Lock button, app start while locked, return to foreground while locked (one attempt per visit, 10 s cooldown against pause/resume loops). Background auto-lock cannot hide immediately (activity starts from background are blocked) → hides on next foreground. Unhide on real unlock only (not decoy); if the marker was lost but the toggle is on, the profile is asked anyway. **Hidden:** all launchable packages in the profile incl. Play Store; **never hidden:** self, GMS/GSF, package installer, permission controller, managed provisioning, settings, systemui, MIUI security/permission/launcher packages (`HidePolicy`, JVM-tested). Profile persists the set it hid; unhide = that set + Play Store safety net; failed unhides of installed packages stay recorded (`partial`) and are retried. Vault reset keeps the "hidden" marker. Quiet mode kept as optional extra "also try to pause" (hide first, then `requestQuietModeEnabled`; unpause + wait before unhiding) — rarely permitted (default launcher only) and greyed icons stay visible, so hiding is the main mechanism. Settings segmented Off/Hide/Quiet control replaced by the two switches. JUnit added as `testImplementation` only. Version 0.3.2+5. | Owner report (Xiaomi): work apps visible on the personal home screen in an "İş" folder. Hiding Play Store doesn't break restore (unhide never needs it; always unhidden on unlock / Open Play Store). Previous rule "auto-lock does not close" is superseded: hiding on the next locked foreground visit is what the owner wants; the trade-off (a running work app is stopped, no notifications while hidden) is documented in docs/SECOND_PHONE_HIDING.md. |
| 2026-09-26 | **Vault home background from a vault photo (#30 PR, owner request):** Gallery long-press opens an options sheet (Open / Set as home background / Export / Delete — new; before there was no long-press). "Set as home background" stores only the photo's item id in `spaces/<space>/home_background.gae` (sealed with the space key), per space. `VaultHomeScreen` decrypts the photo into memory and paints it with a black 45 % `srcATop` scrim; other vault screens and the calculator are untouched. Deleted photo → choice cleared. Settings → "Ana ekran arka planı" shows the state and offers only "Arka planı kaldır"; gradient picker stays as the fallback/default. Lock clears Flutter's image cache. No bundled wallpaper (owner will choose his own): `assets/wallpapers/README.md` reserves the slot, not declared in pubspec. No new permissions. | Owner wants his own photo on the "main menu" only. Keeping the choice inside the encrypted space means neither the photo nor the fact that one is set leaves the vault, and the decoy vault has its own. |
| 2026-09-26 | **Private in-vault browser (#32, both flavors):** vault home tile "Tarayıcı" → `BrowserScreen` on Android System WebView (`webview_flutter` ^4.14, behind an injectable `BrowserEngine` for tests). `INTERNET` added to the main manifest (only new permission; `ACCESS_FINE/COARSE_LOCATION` removed with `tools:node="remove"`; `WebView.MetricsOptOut=true`). Safe Browsing left at WebView default (documented, not toggled). Default search DuckDuckGo, selectable Startpage/Brave/Google/Bing, plain `?q=` URLs, no suggestion API. 1 tab, history in memory. Cookies/cache/storage wiped via `gizlialan/system` `wipeWebData` on lock and bootstrap when "Kilitlenince temizle" (default on) + "Şimdi temizle". Third-party cookies off, file/content access off, geolocation off, all permission requests denied, no JS channels, only http/https/about:blank navigations. **Downloads disabled** (no DownloadListener): encrypted-into-vault download needs a separate fetch with cookie forwarding — not simple, later issue. Feature graphic caption now "No tracking · No ads · Encrypted on device"; all "no internet permission" claims removed from listing/privacy/reviewer docs. Data safety stays "no data collected/shared" (user-initiated browsing). Version 0.4.0+6, prerelease `v0.4.0-test`. | Owner request for an in-vault private browser while staying Play-policy compliant; the app itself still sends nothing. |
| 2026-09-26 | **Vault home signature (#32 PR, owner request):** terminal-style monospace watermark above the dock, 3 lines, ~42 % white + soft shadow (readable on gradient and custom photos): `GizliAlan Vault · v<versionName> (build <versionCode>) · <flavor>` / `AES-256-GCM · PIN: PBKDF2-SHA256 120k · FLAG_SECURE` / mint `●` `vault: unlocked · © OfferForge`. Version/build read at runtime from PackageManager (`gizlialan/system` `appInfo`), compile-time fallback `lib/app_version.dart` (test-checked against pubspec); flavor from `Flavor.current`; cipher bits and PBKDF2 iterations interpolated from `CryptoService` constants (tests assert AesGcm-256 + HMAC-SHA256 in the implementation). Wording: PBKDF2 is the PIN hash (content keys are random and kept in Keystore-backed secure storage), so it says "PIN: PBKDF2", not "KDF". IgnorePointer + ExcludeSemantics; vault home only. Requested during v0.3.2 but shipped in v0.4.0 because v0.3.2-test was already released. | Owner wants a "cool but technical" desktop-style signature; every term must be true for the code. |
| 2026-09-26 | **Default vault home picture (#32 PR, owner request):** owner-supplied, AI-generated with Grok (xAI) image bundled as `assets/wallpapers/default.jpg` (only re-encoded q90 progressive, EXIF stripped, not cropped, 1008×1792, ~310 KB); only asset in pubspec. New setting `wallpaper_image` (default true): vault photo > default picture > gradient; "Arka planı kaldır" returns to the picture; picking a colour swatch = "Düz renk". Readability on the light image: top/bottom `GizliTheme.homeImageScrim`, dark translucent tiles/dock, text shadows; `FilterQuality.medium` for smooth upscale. Attribution "Varsayılan arka plan: Created with Grok" / "Default background: Created with Grok" under the Settings version line (xAI terms: user owns output, commercial use allowed, attribution requested). Provenance in `assets/wallpapers/README.md` and PLAY_COMPLIANCE. Supersedes the #30 rule "no bundled wallpaper". | Owner picked his own default; Play asset provenance must be documentable. |
