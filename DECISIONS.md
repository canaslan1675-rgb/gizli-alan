# Decisions log

| Date (TR) | Decision | Why |
|-----------|----------|-----|
| 2026-09-25 | Architecture = research option C + E: in-app encrypted vault + dual-mode (calculator) UI. No Work Profile, launcher role, app cloning or cloud. | MVP scope per research §6 / brief §1. |
| 2026-09-25 | Encryption always on (removed scaffold's "hide-only" default). AES-256-GCM via `cryptography` (+ `cryptography_flutter` native on Android) instead of pointycastle/`encrypt`. | Pure-Dart pointycastle GCM took ~6 s per 5 MB; `cryptography` Dart impl ~0.6 s and native on device. GCM gives integrity too. |
| 2026-09-25 | One random 256-bit data key per space in Keystore-backed secure storage; not wrapped by PIN. | Allows biometric unlock without PIN; PIN change needs no re-encryption. Limitation documented in PRIVACY/README. Future: biometric-bound Keystore key + PIN-wrapped DEK. |
| 2026-09-25 | PIN = 4–8 digits, PBKDF2-HMAC-SHA256 120k iterations on an isolate; lockout after 5 fails (30 s doubling, cap 15 min). Calculator `=` checks don't count as failures. | Calculator users type normal numbers; counting them would lock the owner out. |
| 2026-09-25 | Decoy PIN opens a separate `decoy` space with its own key; biometrics always open the real space; decoy settings hide security options. | Keepsafe-style decoy, disclosed in listing. |
| 2026-09-25 | Calculator entry on by default in onboarding, with explicit disclosure screen + toggle; launcher label stays "GizliAlan"; ⓘ in calculator explains the entry. Removed scaffold's "4× =" / hidden second-launcher ideas. | Play Deceptive Behavior: listing = behaviour, no cloaking. |
| 2026-09-25 | Removed dart-define `DIRECT_VAULT` second-launcher stub; direct entry = turn calculator off. | Stub never worked per-activity; fewer launcher tricks = lower review risk. |
| 2026-09-25 | Imports via system Photo Picker (`image_picker`) and SAF (`file_picker`), export via SAF "save as"; manifest removes all storage/media/camera/mic permissions. | Minimal permissions; no plaintext temp copy by us (image_picker cache copy is deleted after import). |
| 2026-09-25 | Originals are **not** deleted from the phone gallery after import (user is told). | Deleting needs MediaStore write requests; out of MVP scope. |
| 2026-09-25 | FLAG_SECURE always on (not a toggle). allowBackup=false + data-extraction rules exclude everything. | Keys can't be backed up anyway; backups would be useless blobs. |
| 2026-09-25 | minSdk 28. | BiometricPrompt + FragmentActivity without AppCompat theme issues on ≤8. |
| 2026-09-25 | No billing/IAP code in v1; brief pricing (Free 30–50 items, Pro 249 TL one-time / 449 TL yearly) deferred to a UI stub later. | Brief: real payment integration is a BLOCKER-level step. |
| 2026-09-25 | Language via simple map l10n + `L10nScope` InheritedWidget (no gen-l10n). | Keeps scaffold approach, rebuilds open screens on change. |
| 2026-09-25 | **v0.2:** launcher label → "Hesap Makinesi" (values-tr) / "Calculator" (values, default) via `@string/app_name`; recents title matches. Supersedes "launcher label stays GizliAlan". | Owner request (less conspicuous). Play allows disclosed calculator vaults: the app *is* a working calculator and the vault is disclosed in onboarding step 2, the ⓘ dialog, the store listing and reviewer notes. No icon hiding, no alias switching, no reviewer-specific behaviour. |
| 2026-09-25 | Original calculator icon (navy background, light body, dark display, 3×3 keys, mint "=" key) generated from one geometry by `tool/gen_launcher_icon.py` → adaptive fg/bg + monochrome (themed icons) + round/legacy PNGs + 512 px store icon. | Must not copy Google/Samsung/Xiaomi calculator icons or branding; script keeps vector and PNGs identical and reproducible. |
| 2026-09-25 | **v0.2 "Second phone"** = research option A: managed work profile via `ACTION_PROVISION_MANAGED_PROFILE`; `ProfileAdminReceiver` with empty `<uses-policies>` is profile owner of that profile only. Self-deactivates if enabled as a main-profile device admin. Brief §3 wording clarified accordingly. | Owner request; Shelter/Island model is the only legitimate 3rd-party "second phone". No device-owner/main-profile powers. |
| 2026-09-25 | Main ↔ profile calls go through one cross-profile intent filter to a headless `ProfileActionActivity` (enabled only inside the profile), HMAC-SHA256 signed with a 256-bit secret handed over in `EXTRA_PROVISIONING_ADMIN_EXTRAS_BUNDLE`, 2-min window; caller package checked when available. | Only activities can cross profiles without INTERACT_ACROSS_* permissions. Secret in the provisioning intent avoids Shelter's trust-on-first-use race. |
| 2026-09-25 | App cloning: `installExistingPackage` is tried, but AOSP only allows it for *affiliated* profiles (needs a device owner), so on personal phones it falls back to `enableSystemApp` for system apps and otherwise opens the profile's Play Store page for that app. No `REQUEST_INSTALL_PACKAGES` / APK copying. | Verified in AOSP `DevicePolicyManagerService.installExistingPackage` (affiliation check). Avoids a Play-restricted permission. |
| 2026-09-25 | "Close on lock" option (Off / Hide apps [default] / Turn work profile off). Applied only on the **Lock button** (and back-to-lock), undone on the next real-vault unlock. Quiet mode (`requestQuietModeEnabled`) falls back to hiding apps because Android only lets the default launcher/system do it. Background auto-lock does **not** close the profile. | Auto-lock fires whenever a second-phone app launched from the vault comes to the front; freezing then would kill the app the owner is using. Quiet-mode restriction verified in AOSP `UserManagerService.ensureCanModifyQuietMode`. |
| 2026-09-25 | Second phone is only reachable in the unlocked **real** vault (`secondPhoneIfUnlocked`); decoy vault has no tile and locking it closes nothing. | Gate behind unlock; decoy must not reveal the profile. |
| 2026-09-25 | Profile/clone lists use a `<queries>` MAIN/LAUNCHER intent, not `QUERY_ALL_PACKAGES`; `LauncherApps` for cross-profile listing/launching. No new permissions. | Minimal package visibility; Play-friendly. |
| 2026-09-25 | Xiaomi/Redmi/POCO detected by `Build.MANUFACTURER/BRAND`: warning before setup, blocked message with **Second space** / Android 15+ **Private space** fallback. Generic "not allowed" (existing work profile / OEM) and "unsupported" messages likewise. | MIUI/HyperOS often blocks or half-finishes work-profile provisioning (known Shelter/Island issue). |
| 2026-09-25 | **Play policy risk accepted for testing:** a DeviceAdminReceiver / managed-profile DPC can trigger extra review (Device Admin / "Unwanted software" / Deceptive Behavior checks, esp. combined with a calculator label). Mitigations: disclosure everywhere, empty policies, reviewer notes. Fallback: ship second phone only in a side-loaded APK flavor and keep the Play build vault-only. See BLOCKERS.md. | Owner decides before any Play submission. |
| 2026-09-25 | Version 0.2.0+2. | v0.2 test build. |
| 2026-09-25 | Multi-agent coordination protocol (Joi + Cursor agents): GitHub `task` issues with claim/heartbeat/handoff/24 h takeover labels + append-only `docs/AGENT_LOG.md`; see COORDINATION.md and `.cursor/rules/05-coordination.mdc`. Remaining work filed as issues #4–#17 (owner-only items labelled `owner`). | Owner wants both agent sides working in parallel without direct contact; the repo is the only shared channel. |
| 2026-09-25 | `main` is the integration branch (PRs #1–#3 merged by owner request); all agent branches start from and PR into `main`. | Chained PR lines no longer needed; one base keeps both agent sides in sync. |
| 2026-09-25 | **Vault-only notification list (#4):** encrypted `events.gae` per space (cap 200, newest first) with the app's own events: gallery/files import (count), export and delete (item name), Second phone created / app added / removed, and "N wrong PIN attempts since your last unlock". Home tile with unread badge; mark all read, clear all. The app posts **no system notifications** (no POST_NOTIFICATIONS permission) and never reads other apps' notifications. | Brief week 3 "notification isolation" without new permissions or a Notification Listener (hard ban). Failed attempts use a separate counter that survives decoy unlocks and is reported only inside the real vault, so a decoy user never sees it; calculator `=` checks are not counted. Logging is best-effort and never breaks the action. |
| 2026-09-25 | **Pro UI stub (#5):** Settings → "GizliAlan Pro" lists Free (up to 50 items, no ads), Pro one-time 249 TL (~7.99 USD), Pro yearly 449 TL. Buy/Subscribe only show "not available in this test build". No billing library, no network, no new permissions. Free limit = **50**, **displayed with current usage but not enforced**. Shown in both real and decoy vault settings. | Brief §2 week 3 + §4; real billing is a stop point (#15). Not enforcing avoids locking testers out when there is no way to upgrade. Showing it in the decoy too keeps the decoy looking like a normal vault; it reveals nothing. |
| 2026-09-25 | **Release signing (#8):** `android/app/build.gradle.kts` reads optional `android/key.properties` (storeFile relative to `android/` or absolute, storePassword, keyAlias, keyPassword); present → release `signingConfig` uses it, blank values fail the build; missing → warning + debug key. Template `android/key.properties.example`; `.gitignore` covers key.properties, `*.jks`, `*.keystore`. Recommend Play App Signing. | Standard Flutter pattern; agents/CI can still build test APKs without secrets, and the owner only has to drop in a local file (#13). Verified: no file → `CN=Android Debug`; throwaway keystore → its cert; blank password → clear error. |
| 2026-09-25 | **Docs sync (#9):** ticked brief §2 week 1–3 items verified in code; items done in open PRs (#19, #20, #21) are ticked with the PR number; remaining work linked to issues (#6, #12, #13, #15). §6 rewritten to the current state; §5 marked as standing rules (not ticked); §7 "3 weeks of PR history" left open. No scope, ban or pricing changes. | Brief §7 definition of done needs every item ticked or justified; §6 was stale (GitHub login pending). |
